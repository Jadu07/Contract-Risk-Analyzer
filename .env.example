# Copy this file to `.env` and fill in your keys. The `.env` file is
# loaded at app startup by python-dotenv; real environment variables
# (e.g. set by your hosting platform) always take precedence.

# Google AI Studio — free tier: https://aistudio.google.com/apikey
GEMINI_API_KEY=

# Groq — free tier: https://console.groq.com/keys
GROQ_API_KEY=
